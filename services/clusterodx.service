[Unit]
Description=Start ClusterODX Service

[Service]
Type=simple
PIDFile=/run/clusterodx.pid
User=odx
Group=odx
WorkingDirectory=/www
ExecStart=/usr/bin/node index.js
ExecStop=/bin/kill -s QUIT $MAINPID
Restart=always

[Install]
WantedBy=multi-user.target
